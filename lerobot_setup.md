# LEROBOT setup

To train/test/evaluate our policy with pi0.5 + IMLE, we use the LIBERO benchmark for evaluation

## Setup

Install lerobot

Install pi0.5
```
pip install -e ".[pi]"
```

Install LIBERO
```
pip install -e ".[libero]"
```