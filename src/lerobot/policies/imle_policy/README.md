# IMLE Policy

Pure IMLE policy adapted from https://github.com/krishanrana/imle_policy/

We create customized training loop to train with LIBERO data, but the evaluation
can be done with the lerobot framework.

The IMLE Policy is trained in two modes: per-task mode and all mode. We use that
to show the performance of IMLE Policy on libero dataset.